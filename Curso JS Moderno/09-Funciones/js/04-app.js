const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); // Esto es una funcion nativa de JS

console.log(Number.toString()); // Esto es un metodo de la clase Number

function sumar() {
  console.log(2 + 2);
}

sumar();
